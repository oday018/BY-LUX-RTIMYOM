-- ============================
-- AUTO GRAB - SPEED EDITION
-- ============================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")
local lp = Players.LocalPlayer

-- ============== LUX COLORS ==============
local LUX = {
    Primary = Color3.fromRGB(160, 50, 255),
    Dark = Color3.fromRGB(100, 20, 180),
    Light = Color3.fromRGB(220, 150, 255),
    Accent = Color3.fromRGB(255, 180, 255),
    Red = Color3.fromRGB(255, 50, 50),
    White = Color3.fromRGB(255, 255, 255),
    DarkBG = Color3.fromRGB(25, 10, 45),
    Gray = Color3.fromRGB(60, 60, 60)
}

-- ============== الإعدادات ==============
local GRAB_SPEED = 0.01        -- سرعة خاطفة
local SCAN_DELAY = 0.05        -- كشف سريع جداً
-- =====================================

local grabEnabled = false
local grabConnection = nil

-- Progress Bar
local progressBar, progressFill, percentLabel

-- ============== FUNCTIONS ==============
local function isInsideStealHitbox(root)
    if not root or not root.Parent then return false end
    local plots = Workspace:FindFirstChild("Plots")
    if not plots then return false end
    for _, plot in ipairs(plots:GetChildren()) do
        local hitbox = plot:FindFirstChild("StealHitbox", true)
        if hitbox and hitbox:IsA("BasePart") then
            local parts = Workspace:GetPartsInPart(hitbox)
            for _, p in ipairs(parts) do
                if p:IsDescendantOf(root.Parent) then return true end
            end
        end
    end
    return false
end

local function getPromptPos(prompt)
    if not prompt or not prompt.Parent then return nil end
    local p = prompt.Parent
    if p:IsA("BasePart") then return p.Position end
    if p:IsA("Model") then
        local prim = p.PrimaryPart or p:FindFirstChildWhichIsA("BasePart")
        return prim and prim.Position
    end
    if p:IsA("Attachment") then return p.WorldPosition end
    local part = p:FindFirstChildWhichIsA("BasePart", true)
    return part and part.Position
end

local function findNearestStealPrompt(root)
    if not root then return nil end
    if not isInsideStealHitbox(root) then return nil end
    local plots = Workspace:FindFirstChild("Plots")
    if not plots then return nil end
    local myPos = root.Position
    local nearest = nil
    local nearestDist = math.huge
    for _, plot in ipairs(plots:GetChildren()) do
        for _, obj in ipairs(plot:GetDescendants()) do
            if obj:IsA("ProximityPrompt") and obj.Enabled and obj.ActionText == "Steal" then
                local pos = getPromptPos(obj)
                if pos then
                    local dist = (myPos - pos).Magnitude
                    if dist <= obj.MaxActivationDistance and dist < nearestDist then
                        nearest = obj
                        nearestDist = dist
                    end
                end
            end
        end
    end
    return nearest
end

local function fireStealPrompt(prompt)
    if not prompt then return end
    task.spawn(function()
        pcall(function()
            fireproximityprompt(prompt, 10000)
            prompt:InputHoldBegin()
            task.wait(0.01)
            prompt:InputHoldEnd()
        end)
    end)
end

local function resetBar(hide)
    if progressFill then progressFill.Size = UDim2.new(0,0,1,0) end
    if percentLabel then percentLabel.Text = "0%" end
    if hide and progressBar then progressBar.Visible = false end
end

local function startGrab()
    grabConnection = task.spawn(function()
        while grabEnabled do
            local char = lp.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            
            if root and hum and hum.Health > 0 then
                local prompt = findNearestStealPrompt(root)
                if prompt then
                    progressBar.Visible = true
                    fireStealPrompt(prompt)
                    progressFill.Size = UDim2.new(1,0,1,0)
                    percentLabel.Text = "DONE"
                else
                    resetBar()
                end
            end
            
            task.wait(SCAN_DELAY)
        end
        resetBar(true)
    end)
end

-- ============== GUI ==============
local sg = Instance.new("ScreenGui")
sg.Name = "LUXAutoGrab"
sg.ResetOnSpawn = false
sg.Parent = game:GetService("CoreGui")

-- زر التشغيل
local mainButton = Instance.new("TextButton")
mainButton.Size = UDim2.new(0,140,0,45)
mainButton.Position = UDim2.new(0.5,-70,0.5,-22)
mainButton.BackgroundColor3 = LUX.Primary
mainButton.Text = "✦ AUTO GRAB"
mainButton.Font = Enum.Font.GothamBlack
mainButton.TextSize = 16
mainButton.TextColor3 = LUX.White
mainButton.Parent = sg
local cornerBtn = Instance.new("UICorner", mainButton)
cornerBtn.CornerRadius = UDim.new(0,12)
local strokeBtn = Instance.new("UIStroke", mainButton)
strokeBtn.Color = LUX.Light
strokeBtn.Thickness = 2
local gradBtn = Instance.new("UIGradient", mainButton)
gradBtn.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, LUX.Dark),
    ColorSequenceKeypoint.new(1, LUX.Primary)
})
gradBtn.Rotation = 45
mainButton.Active = true
mainButton.Draggable = true

-- Progress Bar - تحت بـ 20 بكسل
progressBar = Instance.new("Frame")
progressBar.Size = UDim2.new(0,200,0,20)
progressBar.Position = UDim2.new(0.5,-100,0,120)
progressBar.BackgroundColor3 = LUX.DarkBG
progressBar.BackgroundTransparency = 0.2
progressBar.Visible = false
progressBar.Parent = sg
local cornerPB = Instance.new("UICorner", progressBar)
cornerPB.CornerRadius = UDim.new(0,6)
local strokePB = Instance.new("UIStroke", progressBar)
strokePB.Color = LUX.Primary
strokePB.Thickness = 1

progressFill = Instance.new("Frame")
progressFill.Size = UDim2.new(0,0,1,0)
progressFill.BackgroundColor3 = LUX.Primary
progressFill.Parent = progressBar
local cornerPF = Instance.new("UICorner", progressFill)
cornerPF.CornerRadius = UDim.new(0,6)

percentLabel = Instance.new("TextLabel")
percentLabel.Size = UDim2.new(1,0,1,0)
percentLabel.BackgroundTransparency = 1
percentLabel.Font = Enum.Font.GothamBold
percentLabel.TextSize = 12
percentLabel.TextColor3 = LUX.White
percentLabel.Text = "0%"
percentLabel.Parent = progressBar

-- Top Bar
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(0,200,0,25)
topBar.Position = UDim2.new(0.5,-100,0,10)
topBar.BackgroundColor3 = LUX.DarkBG
topBar.BackgroundTransparency = 0.3
topBar.Parent = sg
local cornerTB = Instance.new("UICorner", topBar)
cornerTB.CornerRadius = UDim.new(0,8)
local strokeTB = Instance.new("UIStroke", topBar)
strokeTB.Color = LUX.Primary
strokeTB.Thickness = 1

local topLabel = Instance.new("TextLabel")
topLabel.Size = UDim2.new(1,0,1,0)
topLabel.BackgroundTransparency = 1
topLabel.Font = Enum.Font.GothamBold
topLabel.TextSize = 13
topLabel.TextColor3 = LUX.Light
topLabel.Text = "LUX AUTO GRAB"
topLabel.Parent = topBar

-- ============== BUTTON LOGIC ==============
mainButton.MouseButton1Click:Connect(function()
    grabEnabled = not grabEnabled
    
    if grabEnabled then
        mainButton.Text = "⏹ STOP"
        mainButton.BackgroundColor3 = LUX.Red
        progressBar.Visible = true
        startGrab()
    else
        mainButton.Text = "✦ AUTO GRAB"
        mainButton.BackgroundColor3 = LUX.Primary
        grabEnabled = false
        resetBar(true)
        if grabConnection then
            grabConnection = nil
        end
    end
end)

-- Floating animation
task.spawn(function()
    local baseY = 0.5
    local start = tick()
    while true do
        task.wait()
        local wave = math.sin((tick() - start) * 2) * 0.01
        mainButton.Position = UDim2.new(0.5, -70, baseY + wave, -22)
    end
end)
